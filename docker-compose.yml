services:
  api:
    build: .
    image: infortributos/fiscalia-api
    ports:
      - "${API_PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      - POSTGRES_HOST=172.17.0.1
    restart: unless-stopped
