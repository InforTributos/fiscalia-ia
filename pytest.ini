[pytest]
pythonpath = microservice
markers =
    integration: Test that requires real external services (LLM, DB)
    e2e: End-to-end test using mocked infra + TestClient
    docker: Docker build and run test
testpaths = tests
asyncio_mode = auto
addopts =
    --html=reports/pytest-report.html
    --self-contained-html
